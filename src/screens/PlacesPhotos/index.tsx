import { SafeAreaView, Text } from "react-native";
import Styles from "./styles";

const PlacesPhotos = () => {
    return (
        <SafeAreaView style={Styles.Container}>
            <Text>PlacesPhotos</Text>
        </SafeAreaView>
    );
}

export default PlacesPhotos;