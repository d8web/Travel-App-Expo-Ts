import { SafeAreaView, Text } from "react-native";
import Styles from "./styles";

const PopularLocations = () => {
    return (
        <SafeAreaView style={Styles.Container}>
            <Text>PopularLocations</Text>
        </SafeAreaView>
    );
}

export default PopularLocations;